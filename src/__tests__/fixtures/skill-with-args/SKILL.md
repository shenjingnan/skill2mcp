---
description: Run a test with specific arguments
argument-hint: "<test-file>"
---

# Test Skill

Run tests with the specified arguments.

## Usage

Run the test file specified in $ARGUMENTS:

```!bash
pnpm run test $ARGUMENTS
```

Working directory: ${CLAUDE_SKILL_DIR}
